application:
  migrateCommand: php artisan migrate
  initContainerCommand: php artisan migrate --seed

ingress:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging

prometheus:
  metrics: true
